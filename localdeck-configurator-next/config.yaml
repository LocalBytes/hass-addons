name: "LocalDeck Configurator (Next)"
description: "Configure your LocalBytes LocalDeck with ease!"
slug: "localdeck-configurator-next"
panel_icon: "mdi:dialpad"

image: ghcr.io/localbytes/hass-localdeck-configurator
version: 0.7.1-226-gd2eedb1
stage: beta
arch: [aarch64, amd64]
init: false

schema:
  esphome_dir: str

options:
  esphome_dir: "/homeassistant/esphome"

map:
  - homeassistant_config:rw

ingress: true
ingress_port: 3000

homeassistant_api: true
