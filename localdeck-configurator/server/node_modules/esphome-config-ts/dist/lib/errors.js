export class InvalidConfigError extends Error {
    config;
    constructor(message, config = null) {
        super(message);
        this.name = "InvalidConfig";
        this.config = config;
    }
}
